# Proyecto Integrador Módulo II - Agente Inteligente con LangChain

# Core LangChain (versiones compatibles entre sí)
langchain-classic==1.0.3
langchain-google-genai>=4.0.0
langchain-google-vertexai>=3.0.0
langchain-community>=0.4.0
langchainhub>=0.1.21

# Utilities
python-dotenv>=1.0.0

# Procesamiento de PDFs
pypdf>=4.0.0

# Vector Store — FAISS (búsqueda semántica local, sin servidor)
faiss-cpu>=1.7.4

# Google Drive API + Service Account (descarga de documentos corporativos)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Google Cloud Platform — Vertex AI
google-cloud-aiplatform>=1.60.0

# Google Cloud Storage — PDFs y vectorstore en GCS
google-cloud-storage>=2.14.0

# API REST (FastAPI + Uvicorn para Cloud Run)
fastapi>=0.104.0
uvicorn>=0.24.0
